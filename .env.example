# AWS Credentials for Bedrock verification (Claude) & Embedding (Titan)
AWS_ACCESS_KEY_ID="your_access_key"
AWS_SECRET_ACCESS_KEY="your_secret_key"
AWS_DEFAULT_REGION="ap-south-1"  # Or your region (e.g., us-east-1)

# Optional: Elasticsearch Host (Default: localhost)
# ELASTICSEARCH_HOST="localhost"
